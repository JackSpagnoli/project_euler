use project_euler::*;

fn main() {
    assert_eq!(problem035::ans(), 55);
    assert_eq!(problem036::ans(), 872187);
    assert_eq!(problem037::ans(), 748317);
    assert_eq!(problem038::ans(), 932718654);
    assert_eq!(problem039::ans(), 840);
    assert_eq!(problem040::ans(), 210);
    assert_eq!(problem041::ans(), 7652413);
    assert_eq!(problem042::ans(), 162);
    assert_eq!(problem043::ans(), 16695334890);
    assert_eq!(problem044::ans(), 5482660);
    assert_eq!(problem045::ans(), 1533776805);
    assert_eq!(problem046::ans(), 5777);
    assert_eq!(problem047::ans(), 134043);
    assert_eq!(problem048::ans(), 9110846700);
    assert_eq!(problem049::ans(), 296962999629);
    assert_eq!(problem050::ans(), 997651);
    // assert_eq!(problem051::ans(), 121313);
    assert_eq!(problem052::ans(), 142857);
    assert_eq!(problem053::ans(), 4075);
    assert_eq!(problem054::ans(), 376);
    assert_eq!(problem055::ans(), 249);
}
